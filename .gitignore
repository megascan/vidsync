node_modules
dist
.astro
.wrangler
.dev.vars
.env
.env.*
!.env.example
*.log
.DS_Store
.turbo
coverage

# Package manager lockfiles (bun/npm/yarn/pnpm — not committed)
bun.lock
bun.lockb
package-lock.json
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml

# Rust
**/target/

# Tauri updater signing keys (NEVER commit private key)
.keys/
