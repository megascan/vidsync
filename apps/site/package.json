{
  "name": "@vidsync/site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node -e \"console.log('vidsync-web: static public/ + worker.ts, nothing to compile')\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
